# Sum of Lengths

Write a function 'sum_of_lengths' that takes in an array of strings and returns
the total length of the strings. 

You must solve this recursively.

## Running tests

```bash
cd build && \
make && \
./test_solution
```
